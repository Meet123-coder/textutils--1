import React from 'react'

export default function About(props) {
  const textColor = props.mode === 'dark' ? 'white' : '#042743'
  const bgColor = props.mode === 'dark' ? '#0a2339ff' : 'white'

  return (
    <></>
  )
}

